# Admin credentials — set these in Netlify env vars (never commit real values)
ADMIN_USERNAME=your-username
ADMIN_PASSWORD=your-password

# Token secret for session tokens (any random string)
TOKEN_SECRET=replace-with-a-random-string
